on:
  schedule:
    - cron: '0 0 * * 0' # Each Sunday Midnight
  workflow_dispatch: # Allow manual trigger

jobs:
  # update-deps:
  #   name: Dependencies Update
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 30
  #   steps:
  #   - uses: actions/checkout@v6
  #   - uses: cachix/install-nix-action@v31
  #     with:
  #       github_access_token: ${{ secrets.GITHUB_TOKEN }}
  #       extra_nix_config: |
  #         extra-substituters = https://cache.garnix.io
  #         extra-trusted-public-keys = cache.garnix.io:CTFPyKSLcx5RMJKfLo5EEPUObbA78b0YQ2DTCJXqr9g=
  #   - uses: DeterminateSystems/magic-nix-cache-action@v13
  #   - run: nix flake update
  #   - run: nix develop -c cargo update
  #   TODO Without rebuilding devShell instead of Garnix

  cargo-deny:
    name: Dependencies Audit
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: cachix/install-nix-action@v31
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}
          extra_nix_config: |
            extra-substituters = https://cache.garnix.io
            extra-trusted-public-keys = cache.garnix.io:CTFPyKSLcx5RMJKfLo5EEPUObbA78b0YQ2DTCJXqr9g=
      - uses: DeterminateSystems/magic-nix-cache-action@v13
      - run: nix develop -c cargo deny check

  cargo-mutants:
    name: Mutation Testing
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v6
      - uses: cachix/install-nix-action@v31
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}
          extra_nix_config: |
            extra-substituters = https://cache.garnix.io
            extra-trusted-public-keys = cache.garnix.io:CTFPyKSLcx5RMJKfLo5EEPUObbA78b0YQ2DTCJXqr9g=
      - uses: DeterminateSystems/magic-nix-cache-action@v13
      - run: nix develop -c cargo mutants --no-shuffle --caught --timeout 300 \
          --exclude "src/components/*"

  # e2e-android:
  #   name: Android End-to-End Tests
  #   runs-on: macos-latest
  #   timeout-minutes: 45
  #   steps:
  #     - uses: actions/checkout@v6
  #     - uses: cachix/install-nix-action@v31
  #       with:
  #         github_access_token: ${{ secrets.GITHUB_TOKEN }}
  #         extra_nix_config: |
  #           extra-substituters = https://cache.garnix.io
  #           extra-trusted-public-keys = cache.garnix.io:CTFPyKSLcx5RMJKfLo5EEPUObbA78b0YQ2DTCJXqr9g=
  #     - uses: DeterminateSystems/magic-nix-cache-action@v13
  #     - run: TODO With package built in CD
  #     - uses: reactivecircus/android-emulator-runner@v2
  #       with:
  #         api-level: 33
  #         target: google_apis
  #         arch: arm64-v8a TODO Consider using x86_64 build, easier to emulate?
  #         force-avd-creation: false
  #         emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
  #         disable-animations: false
  #         script: TODO With package built in CD
  #     - if: failure()
  #       uses: actions/upload-artifact@v7
  #       with:
  #         name: maestro-android-report
  #         path: ~/.maestro/tests
